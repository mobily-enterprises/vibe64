# Vibe64 Directory Model

The canonical directory model now lives in the official developer manual:

```text
docs/site/dev/technical-reference.md
```

Keep this file as a pointer only. Directory policy should have one documented
source of truth so the internal notes and the public manual do not drift apart.
