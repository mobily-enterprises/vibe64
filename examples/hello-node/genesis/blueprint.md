# Blueprint

Hello Node is a small web application for people learning to edit and run a
project in Vibe64. Its first useful outcome is a running welcome page that they
can change in Files and see after refreshing Preview.

The page includes a click counter that resets on reload. It needs no accounts,
database, external services, or installed application dependencies. Project
source and its Genesis explanations remain usable outside the editor.
