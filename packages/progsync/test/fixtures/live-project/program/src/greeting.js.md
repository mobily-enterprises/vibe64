# Greeting

Creates a personal greeting without external effects.

## Uses

- Nothing outside this file.

## Provides

### `greet()`

The function takes `name`, text containing a person's name, and
returns greeting text.

It returns `Hello, ` followed by `name` and `!`.
