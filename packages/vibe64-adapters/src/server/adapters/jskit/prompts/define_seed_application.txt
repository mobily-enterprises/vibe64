{{systemStandard}}

JSKIT seed recipe contract:
{{adapter.promptContext.seed_recipe_contract}}

JSKIT seed guidance:
{{adapter.promptContext.seed_issue_guidance}}

Keep the seed description command-first and framework-owned. It should identify the JSKIT app creation command, selected JSKIT modules/packages, Vibe64-provided database/auth runtime choices, any user-provided setup value such as an OpenAI API key, dependency install expectations, verification path, and the smallest visible app workflow to implement.

If the seed creates or changes browser-visible UI, acceptance criteria must include a minimal Playwright/browser workflow and the JSKIT UI verification receipt step before final verification.

JSKIT UI verification contract:
{{adapter.promptContext.ui_verification_contract}}

Do not define unrelated product CRUD entities, detailed screens, or feature implementation beyond the smallest usable workflow and the foundation required by the selected modules. If no database was selected, describe the workflow with browser-local state instead of deferring it.
