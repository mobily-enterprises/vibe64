{{systemStandard}}

Execute the JSKIT seed plan.

JSKIT seed guidance:
{{adapter.promptContext.seed_issue_guidance}}

Use official JSKIT commands for JSKIT-owned files and package/module wiring. Use Vibe64-provided environment variables for database/auth setup. Write only genuinely user-provided local development values, such as an OpenAI API key, to ignored .env files when needed. After the scaffold exists, install dependencies and run the mapped JSKIT commands from the accepted plan.

For current-directory scaffolding, use `npx @jskit-ai/create-app <app-name> --target . --force --tenancy-mode none --title "<app title>" --initial-bundles none`. Do not use `npx @jskit-ai/create-app . --name ...`, and do not create or keep a nested app directory.

After scaffolding, prefer generated scripts and exact JSKIT commands from the seed plan: `npm install`, the selected `npx jskit add ...` / `npx jskit generate ...` commands, `npm run build`, generated verification scripts such as `npm run verify` when present, targeted tests, and the JSKIT UI verification receipt flow when UI files changed. Do not browse the catalog or run broad listing commands during normal seed execution; use `npx jskit ... help` only for one selected command whose syntax is genuinely unclear. Use `npx jskit ...`, not a bare global `jskit`.

JSKIT UI verification contract:
{{adapter.promptContext.ui_verification_contract}}

Do not add app-local `optimizeDeps` entries for JSKIT package internals. If a generated app exposes a Vite dev optimization failure involving JSKIT runtime packages, report it as a JSKIT package metadata/update issue and keep the generated Vite config framework-owned.

Keep the result to a runnable foundation app with the smallest visible app workflow from the accepted plan. Do not start unrelated product feature work. If no database was selected, use browser-local state for the visible workflow instead of adding database scaffolding or deferring the behavior.
