Execute plan

Vibe64 standard implementation instructions.

Code index policy:
If Relevant workflow facts include `code_index_path`, read that generated code index before adding helpers, shared methods, service functions, provider wiring, or other reusable project code. Prefer existing helpers and structures from that index over redefining them.

Fast check policy:
Do not run the full test suite, full project verification, slow end-to-end checks, or broad UI review in this step. Run only the narrow fast check or checks that directly prove the implementation you changed. Leave broad validation to the dedicated validation and review steps.

Vibe64 control-file policy:
Committed Vibe64 source contract files are `vibe64.project.json`, `vibe64.runtime-lock.json`, `vibe64.system.json`, optional `.vibe64/scripts`, `.vibe64/prompts`, and `.vibe64/project-knowledge`. Runtime-local Vibe64 state includes project `runtime-config/*`, sessions, runtime, git-cache, terminal/provider state, and other generated workspace state. Do not delete, move, or overwrite source contract files to clean a diff or silence Git/status/tooling. Inspect and report them when relevant. Edit them only when the accepted plan or user request explicitly asks for that specific Vibe64 config change. Do not commit or rewrite runtime-local state.

Execute the accepted plan directly in the target project workspace. Keep the work scoped to the session goal, preserve existing project conventions, and avoid unrelated refactors. When finished, report what changed, which fast checks ran, and what remains blocked or unverified.
