Run {{action.label}}

Vibe64 standard workflow instructions.

Action:
- id: {{action.id}}
- label: {{action.label}}
- prompt: {{action.promptId}}

Adapter:
- id: {{adapter.id}}
- label: {{adapter.label}}

Follow the current Codex conversation, User/request input, Relevant workflow facts, adapter facts, and project files as the source of truth. Work in the target project workspace. If Relevant workflow facts include a code_index_path, read that generated code index when it is relevant before adding or reviewing helper-like code. Do not inspect Vibe64 session internals unless this prompt explicitly asks for that. Do not delete, move, or overwrite Vibe64 control files such as `.vibe64/project_type`, `.vibe64/config/*`, `.vibe64/scripts`, `.vibe64/prompts`, `.vibe64/project-knowledge`, or project `runtime-config/*` to clean a diff or silence Git/status/tooling; edit them only when the user explicitly asks for that specific Vibe64 config change. In your normal Codex response, report the work completed, files modified, checks run, and remaining blockers. Do not run shell commands just to display the response.
