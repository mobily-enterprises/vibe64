Run {{action.label}}

Vibe64 standard workflow instructions.

Action:
- id: {{action.id}}
- label: {{action.label}}
- prompt: {{action.promptId}}

Adapter:
- id: {{adapter.id}}
- label: {{adapter.label}}

Follow the current agent conversation, User/request input, Relevant workflow facts, adapter facts, and project files as the source of truth. Work in the target project workspace. If Relevant workflow facts include a code_index_path, read that generated code index when it is relevant before adding or reviewing helper-like code. Do not inspect Vibe64 session internals unless this prompt explicitly asks for that. In your normal agent response, report the work completed, files modified, checks run, and remaining blockers. Do not run shell commands just to display the response.
