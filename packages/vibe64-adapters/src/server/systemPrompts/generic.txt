Run {{action.label}}

Vibe64 standard workflow instructions.

Action:
- id: {{action.id}}
- label: {{action.label}}
- prompt: {{action.promptId}}

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- status: {{session.status}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}
- artifacts root: {{session.artifactsRoot}}

Session metadata:
{{session.metadata.json}}

Adapter:
- id: {{adapter.id}}
- label: {{adapter.label}}

{{prompt.sessionBriefingReference}}

Action input:
{{input.json}}

Missing information policy:
{{prompt.missingInformationPolicy}}

Follow the current Codex conversation, Action input, session metadata, adapter facts, and project files as the source of truth. Work in the target project workspace. If session metadata includes a code_index_path, read that generated code index when it is relevant before adding or reviewing helper-like code. Do not inspect Vibe64 session internals unless this prompt explicitly asks for that. In your normal Codex response, report the work completed, files modified, checks run, and remaining blockers. Do not run shell commands just to display the response.
