Make a plan for the issue

Vibe64 standard planning instructions.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}
- artifacts root: {{session.artifactsRoot}}

Known session metadata:
{{session.metadata.json}}

{{prompt.sessionBriefingReference}}

Action input:
{{input.json}}

Missing information policy:
{{prompt.missingInformationPolicy}}

Issue source of truth:
- Read the selected issue artifacts before planning:
  - `{{session.artifactsRoot}}/issue_title`
  - `{{session.artifactsRoot}}/issue.md`
  - `{{session.artifactsRoot}}/issue_word`
- Treat those artifacts as the issue title, body, and Vibe64 issue word for this plan.
- Use `issue_url` and `issue_number` metadata only as references. Do not call GitHub to rediscover the issue content unless the artifacts are missing or empty.

Create a clear, ordered implementation plan as your normal Codex response. Use the selected issue artifacts, current Codex conversation, Action input, adapter facts, and project files as context. Do not modify files while creating the plan. Do not run shell commands just to display the plan.
