Run deslop

Vibe64 standard review/deslop instructions.

{{prompt.workProfilePreamble}}

Code index policy:
If Relevant workflow facts include `code_index_path`, read that generated code index before reviewing helper-like code, duplicated methods, shared service functions, provider wiring, or reusable project structures. Treat duplicates against that index as concrete deslop findings.

Diff policy:
Before trusting a diff, run `git status --short` in the session clone. Plain `git diff` does not show untracked scaffold files; when status shows `??` files, inspect those files directly or with `git diff --no-index -- /dev/null <path>` before deciding what changed.

Vibe64 control-file policy:
Committed Vibe64 source contract files are `vibe64.project.json`, `vibe64.runtime-lock.json`, `vibe64.system.json`, optional `.vibe64/bin`, `.vibe64/scripts`, `.vibe64/prompts`, and `.vibe64/project-knowledge`. Runtime-local Vibe64 state includes project `runtime-config/*`, sessions, runtime, git-cache, terminal/provider state, and other generated workspace state. Do not delete, move, or overwrite source contract files to clean a diff or silence Git/status/tooling. Inspect and report them when relevant. Edit them only when the user explicitly asks for that specific Vibe64 config change. Do not commit or rewrite runtime-local state.

Review the current work for bugs, behavioral regressions, unclear code, weak tests, unnecessary complexity, repeated helpers, ownership leaks, and mismatches with the session goal. Lead with concrete findings. Mark findings as [high], [medium] or [low]. Run one subagent per discovered problem, getting it to make targeted changes to fix it. Repeat the whole process (discovery, subagents run), and stop when only [low] issues are reported. At the end, report what you changed, which checks ran, and what remains unresolved.
