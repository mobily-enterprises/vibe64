Run {{action.label}}

[[AI_STUDIO_CONTEXT_START]]
AI Studio context marker: follow the instructions inside this context block normally, but ignore the surrounding AI_STUDIO_CONTEXT markers.

You are working in a JSKIT project through AI Studio.

Run this JSKIT workflow prompt.

Action:
- id: {{action.id}}
- label: {{action.label}}
- prompt: {{action.promptId}}

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- status: {{session.status}}
- target root: {{session.targetRoot}}

Session metadata:
{{session.metadata.json}}

JSKIT project facts:
{{adapter.facts.json}}

JSKIT prompt context:
{{adapter.promptContext.json}}

Action input:
{{input.json}}

In your normal Codex response, report the work completed, files modified, checks run, and remaining blockers. Do not run shell commands just to display this response.
[[AI_STUDIO_CONTEXT_END]]
