{{systemStandard}}

JSKIT-specific deslop-resolution guidance:
- Resolve outstanding findings without broadening the issue.
- Keep provider, command, CRUD/resource, generated surface, setup, and blueprint behavior internally consistent.
- If the finding is hand-built JSKIT-owned files, replace them with JSKIT generator/module output where the generator path applies.
- If the finding is improper placement work, read the agent-friendly placement docs, use JSKIT semantic placement conventions, and avoid manual `src/placement.js` changes unless unavoidable.
- If the finding is ad hoc persistence for durable app data, replace it with JSKIT database/runtime modules, migrations, and generated resource/CRUD scaffolding where the configured database runtime applies.
- If the finding is a hand-written migration, table without generated CRUD, or direct Knex feature access, create/update the table first, run the server-side CRUD generator, and route feature access through generated JSKIT/json-rest-api APIs.
- Re-run only the focused fast checks that prove the resolved findings.

JSKIT guide-first contract:
{{adapter.promptContext.agent_guide_contract}}

JSKIT generated-file contract:
{{adapter.promptContext.tooling_contract}}

JSKIT placement contract:
{{adapter.promptContext.placement_contract}}

JSKIT database contract:
{{adapter.promptContext.database_contract}}
