Create issue file

[[AI_STUDIO_CONTEXT_START]]
AI Studio context marker: follow the instructions inside this context block normally, but ignore the surrounding AI_STUDIO_CONTEXT markers.

Create the local issue files for this Next.js project.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}
- artifacts root: {{session.artifactsRoot}}

Next.js project facts:
{{adapter.facts.json}}

Next.js prompt context:
{{adapter.promptContext.json}}

Next.js project config:
{{config.json}}

Next.js data layer blueprint:
{{adapter.promptContext.data_layer_blueprint}}

Action input:
{{input.json}}

Use package.json, Next.js scripts, and Next.js project files if project inspection is useful. Use the current Codex conversation and Action input as the source of truth for the issue. Do not inspect AI Studio session internals, session files, or existing artifact files.

Write the reviewed issue title to:
{{session.artifactsRoot}}/issue_title

Write the reviewed issue body to:
{{session.artifactsRoot}}/issue.md

Keep the issue focused on the scoped work and call out Next.js routing, runtime, or deployment risk when relevant.
[[AI_STUDIO_CONTEXT_END]]
