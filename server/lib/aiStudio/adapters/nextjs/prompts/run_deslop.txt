Run deslop

[[AI_STUDIO_CONTEXT_START]]
AI Studio context marker: follow the instructions inside this context block normally, but ignore the surrounding AI_STUDIO_CONTEXT markers.

Run the review/deslop pass for this Next.js project.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}

Known session metadata:
{{session.metadata.json}}

Next.js project facts:
{{adapter.facts.json}}

Next.js prompt context:
{{adapter.promptContext.json}}

Next.js project config:
{{config.json}}

Next.js data layer blueprint:
{{adapter.promptContext.data_layer_blueprint}}

Action input:
{{input.json}}

Review the current work for bugs, regressions, unclear code, weak tests, unnecessary complexity, and Next.js routing or runtime risk. Check Server Component vs Client Component boundaries, route handler behavior, package scripts, and selected database runtime assumptions. Report findings clearly and make direct fixes when the issue is unambiguous.
[[AI_STUDIO_CONTEXT_END]]
