Define issue

[[AI_STUDIO_CONTEXT_START]]
AI Studio context marker: follow the instructions inside this context block normally, but ignore the surrounding AI_STUDIO_CONTEXT markers.

Help define the GitHub issue for this Next.js project.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}

Next.js project facts:
{{adapter.facts.json}}

Next.js prompt context:
{{adapter.promptContext.json}}

Next.js project config:
{{config.json}}

Next.js data layer blueprint:
{{adapter.promptContext.data_layer_blueprint}}

Action input:
{{input.json}}

The user's issue request is the issueRequest value in Action input.

Use package.json, Next.js scripts, and routing files if project inspection is useful. Do not inspect AI Studio session internals, session files, or artifact files.

Clarify the requested issue until it is scoped enough to create a concise issue title and issue body. Do not modify files.
[[AI_STUDIO_CONTEXT_END]]
