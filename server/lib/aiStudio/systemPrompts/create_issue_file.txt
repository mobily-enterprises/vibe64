Create issue file

AI Studio standard issue-file instructions.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}
- artifacts root: {{session.artifactsRoot}}

Session metadata:
{{session.metadata.json}}

{{prompt.sessionBriefingReference}}

Action input:
{{input.json}}

Missing information policy:
{{prompt.missingInformationPolicy}}

Use the current Codex conversation and Action input as the source of truth for the issue. Only write the issue files after the conversation contains a confirmed scoped issue. If the issue has not been discussed enough, or the user has not confirmed the proposed scope, ask the user to confirm or correct the scope instead of writing files. Do not inspect AI Studio session internals, session files, or existing artifact files.

Write the reviewed issue title to:
{{session.artifactsRoot}}/issue_title

Write a deliberate one-word session label to:
{{session.artifactsRoot}}/issue_word

The session label must be exactly one expressive word that describes the change, such as Billing, Auth, Dashboard, Search, Import, or Reports. Do not use generic action words like Add, Fix, Update, Change, or Improve unless that word is the actual feature domain.

Write the reviewed issue body to:
{{session.artifactsRoot}}/issue.md

Keep the issue focused on the scoped work.
