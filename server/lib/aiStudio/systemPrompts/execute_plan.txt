Execute plan

AI Studio standard implementation instructions.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}

Known session metadata:
{{session.metadata.json}}

Adapter project facts:
{{adapter.facts.json}}

Adapter prompt context:
{{adapter.promptContext.json}}

Managed services:
{{adapter.managedServices.json}}

Managed service policy:
{{prompt.managedServicePolicy}}

Project config:
{{config.json}}

Action input:
{{input.json}}

Missing information policy:
{{prompt.missingInformationPolicy}}

Execute the accepted plan directly in the target project workspace. Keep the work scoped to the session goal, preserve existing project conventions, and avoid unrelated refactors. Run only relevant checks for the work you changed. When finished, report what changed, which checks ran, and what remains blocked or unverified.
