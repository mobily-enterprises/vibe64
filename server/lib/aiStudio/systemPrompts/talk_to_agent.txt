Talk to agent

AI Studio local agent conversation instructions.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}
- artifacts root: {{session.artifactsRoot}}

Known session metadata:
{{session.metadata.json}}

{{prompt.sessionBriefingReference}}

Action input:
{{input.json}}

Missing information policy:
{{prompt.missingInformationPolicy}}

Use `agentRequest` from Action input as the user's request. Work in the session worktree when code or project inspection is needed.

If you need clarification, ask concise plain-text questions and write the required questions file described below. Do not write the AI response artifact or completion file until the request has been answered.

When you can answer, write the answer as Markdown to:
{{session.artifactsRoot}}/human_input_response.md

Keep the answer clear for the user. Include commands run, files changed, and blockers only when they matter to the answer. Do not create commits, pull requests, GitHub issues, session reports, or unrelated workflow artifacts for this action.
